{
  "name": "loginext-ui",
  "version": "0.1.0",
  "private": true,
  "description": "LogiNext control panel — neumorphism-dark Tauri frontend.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "WEBKIT_DISABLE_DMABUF_RENDERER=1 tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
